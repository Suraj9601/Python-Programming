# 36. Input base and height, calculate area of triangle.

base = float(input("Enter the base of triangle : "))
height = float(input("Enter the height of triangle :"))

area = base * height
print("Area of triangle is ", area)
