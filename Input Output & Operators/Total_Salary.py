# 60. Calculate total salary with bonus (base + bonus).

base = float(input("Enter the base salary amount : "))
bonus = float(input("Enter the bonus amount : "))

total_salary = base + bonus

print("Total Salary : ", total_salary)
